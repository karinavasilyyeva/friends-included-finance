{
  "name": "friends-included-finance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-isolation=none",
    "dev": "npx vercel dev"
  },
  "engines": { "node": ">=20" }
}
